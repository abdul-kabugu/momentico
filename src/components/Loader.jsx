

const Loader = ({title}) => (
  <div className="w-full justify-center items-center h-screen">
    <p className="text-lg text-white">The post  is Loading  {title}  </p>
  </div>
);

export default Loader;
